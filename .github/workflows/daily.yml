name: Daily MLB Data Update

on:
  schedule:
    # Runs automatically every day at 10:00 AM UTC
    - cron: '0 10 * * *'
  workflow_dispatch: # Allows you to manually run it anytime with a button click

jobs:
  update-mlb-data:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository code
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install requests

      - name: Run MLB Data Fetcher
        run: python fetch_data.py

      - name: Commit and push changes back to repo
        run: |
          git config --global user.name "github-actions[bot]"
          git config --global user.email "github-actions[bot]@users.noreply.github.com"
          git add daily_mlb_data.json
          git commit -m "Auto-update MLB data: $(date)" || exit 0
          git push
